mode: development

log:
  file: ./logs/app.log
  level: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: postgres
  sslmode: disable

shopify:
  api_key:
  shared_secret:
  redirect_uri:
  scope:

redis:
  host: localhost
  port: 6379
  db: 0
  pool_size: 10
